# Copy to .env.local for local dev, or set on Railway (SMART-MONITORING service).
# Laravel public origin only — no trailing slash, no /api suffix.
REACT_APP_API_BASE=https://your-laravel-service.up.railway.app
